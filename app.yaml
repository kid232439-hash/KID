runtime: python312
entrypoint: uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT
env_variables:
  ENVIRONMENT: production
  JWT_SECRET: replace-with-secret-manager-value
  FREERADIUS_SHARED_SECRET: replace-with-secret-manager-value
  PAYMENT_WEBHOOK_SECRET: replace-with-secret-manager-value
  SATELLITE_API_KEY: replace-with-secret-manager-value
